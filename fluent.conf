<source>
  type forward
  port 24224
  bind 0.0.0.0
</source>

<match *>
  type stdout
</match>

<match elasticsearch>
  type copy
  <store>
    type stdout
  </store>
  <store>
    type elasticsearch
    logstash_format true
    flush_interval 5s
  </store>
</match>